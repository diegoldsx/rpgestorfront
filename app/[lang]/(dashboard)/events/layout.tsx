// app/dashboard/agenda/layout.tsx
export const metadata = {
	title: 'Calendário de Eventos',
};

const Layout = ({ children }: { children: React.ReactNode }) => {
	return <>{children}</>;
};

export default Layout;
